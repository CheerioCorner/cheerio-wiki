---
title: "MiniCPM5-2B: The Best Sub-Agent Model Yet?"
type: raw-transcript
created: 2025-07-17
video_id: CXvncR7v66o
url: https://www.youtube.com/watch?v=CXvncR7v66o
duration: 00:14:31
language: English (auto-generated)
auto_generated: true
source_api: youtube_transcript_api
segments: 41
timestamp_format: "[MM:SS]"
---

# MiniCPM5-2B: The Best Sub-Agent Model Yet?

> Duration: 00:14:31 | Segments: 41 | Language: English (auto-generated) | Source: API (auto-generated)

## Transcript

[00:00] Okay, so a few months ago MiniCPMQ released their 1B model and I covered this on the channel talking about the whole concept of sort of having a small cognitive model that could do a lot of different things. Well, after a few months they are back with a new model and they've jumped from 1B to 2B here.
[00:21] In fact, I think it's actually a 2.5B model, but we'll give them that. So, this is the second model in this mini series from OpenBMB. And in many ways they've taken the same sort of recipe, but they've scaled it from 1B to 2B. So, I guess the big question here is when you scale the data and post training recipe by 2x, does that suddenly make this model a lot more viable for the hard function calling and along with that 2x scaling, does the agentic stuff scale as well? So, the interesting thing here is that the 1B model was pitched at
[00:58] sort of coding agents and tools use. The 2B model is claiming to be 4B models at the same kind of thing. If we come in here and look at their benchmarks, we can see that they're edging out Qwen 3.5B on things like SweetBench verified, etc.
[01:15] Now, when we look at the training recipe, it seems to be mostly the same and it seems that these scaling improvements really did sort of have an effect here. In fact, in some places more than I expected. So, let's take a look at this stuff. So, where the supervised fine-tuning for the 1B was 200 billion deep thinking tokens and 200 billion hybrid tokens, the 2B has gone for 400 billion tokens of deep thinking only. But, we can see where some of these changes have come in both the RL and the on-policy distillation here. We
[01:48] can see that they've changed up how they're doing the RL stage. So, they've got this new thing called RL2. It looks like they're going to release a whole paper about this. This is just sort of like an early pre-print here. And the key thing here is that they've changed how they're doing this here to basically include a critic in the RL as well. All right, so if we take a look at their training recipe, we can see that they have this sort of pre-training and mid-training here. And we can see that that's basically setting up the model
[02:16] for the post-training processes. So if we look at the post-training, the post-training is breaking down to three different parts, right? You've got your standard SFT. This is sort of providing the format for outputs, providing sort of some of the key thinking behavior, etc. They then do RL per domain. So they've got multiple experts that they're doing the RL and the on-policy distillation stuff from there. And we can see that those are sort of breaking down into sort of reasoning task RL, general task RL, and agentic stuff
[02:49] there. And the key thing with that is that each of those experts only needs to be good at one thing. This is the new form of RL here, this critic-based just RL2. And then they're claiming that this actually has better stability than the sort of traditional GRPO stuff. But it's interesting that this is showing here that this makes the model a lot better on a lot of their different benchmarks.
[03:14] Now finally, they have their on-policy distillation layer. Here the model is sampling out its own trajectories that were used to train the teacher models.
[03:22] And then from that, they can then basically look at how different it is to the teacher models and then update in a loop, etc. So when we go back into the benchmarks, they're doing a lot better than obviously a lot of the 2B models, but they're doing very well compared to a lot of the 4B models in here. And so the trade-off here is that it's not a huge amount to go from 1 to 2B if we're getting a model that's substantially better and now also has things like general knowledge in it. If we come down
[03:51] and look at the things like the GBQA diamond, etc., we've got a model now that, okay, is a bit behind the 4B model, but this is way ahead of the other sort of 2B models that are out there. And of course, it's miles behind the frontier models that are well up into the high 90s, etc., at this point, but not that long ago, this kind of score was considered a good score for models that were 10x to 100x bigger than this particular model. Now, the cool thing here is that they've released a bunch of different versions of this
[04:24] model. You've got sort of the final model that's got everything in it.
[04:28] You've got just the SFT models before RL. You've got mid-training models.
[04:33] You've got the base model in there. And then you've got a bunch of quantizations and a deep spark model in here, which is good for the speculative decoding, etc.
[04:42] Of course, one of the good things about this size of model is that it's very easy to fine-tune for your specific use case. Then you face the challenge of what if you wanted to have that fine-tune on whenever you wanted to access that? And that actually brings me to today's sponsor. So, one of the things that you often want to do is get a model like this set up as an OpenAI compatible API that scales. And that's where RunPod Serverless comes in. So, thanks to them for sponsoring this video. Let me just show you how quickly
[05:09] you can go from having a model on Hugging Face to being able to deploy it to VL on a serverless endpoint with a GPU that you choose. So, you can see here that I've picked the model from Hugging Face. I now have to pick a GPU with enough memory to run it on. Then I can just set my scaling limits and create an endpoint. Once it's ready, I can now send a prompt and the model returns a response. It's literally that easy to have an API that my applications or my agents can call. Behind the scenes, RunPod routes the request to a
[05:40] worker running my model on a GPU. And if demand grows, RunPod can automatically scale up those workers within the limits that I've set. And if I don't use it for a while, it can scale it back down to zero. The cool thing here is whenever I ping a new request, the model will spin up and then be able to serve it.
[05:58] Computers billed by the second while workers run, so you don't have to keep a GPU rented around the clock. They also have flash boot, which helps reduce cold starts. And if responsiveness is the key priority, I can actually pay to keep the workers running and ready all the time.
[06:14] So, all of this just gives you the control to serve the model where the GPU infrastructure is all managed for you, and you only have to pay for the time that you're actually using it. If you're building any AI app or deploying your own model, check out RunPod Serverless at the link below and get started today.
[06:30] All right, so did everything actually scale here and this clearly is beating 4B models? Actually, the answer is no.
[06:36] If we look at things like SweetBench Pro, we can see that the Qwen model is substantially better than this, basically double it. And if we look at Terminal Bench, we can see that the model is way behind the Qwen model. But, it still is a lot further advanced than a lot of the other 4B models out there.
[06:54] And don't forget that the Qwen models probably have a much bigger budget for pre-training, for a lot more tokens, for a lot more things like that.
[07:02] Unfortunately, we haven't seen the really small Qwen models being updated to say the 3.8 models. Hopefully, that's going to come with the not too far off Qwen 4 models. So, let's jump in and have a look at the speed of this of running it locally. Have a look at just how it actually does on some of the function calling and some of the agentic tasks in my sort of testing suite. All right, so I've got three versions of the model running here. I've got the BFloat16 version, I've got a DeepSpark, so basically a speculative decoding
[07:32] version, and I've got the 4-bit GGUF version with llama.cpp. I'm running the two bigger versions on SG Lang here, and you can see that if I kick it off, I'm actually getting pretty good token speech, which is to be expected. I'm running all of these on an RTX Pro 6000.
[07:51] Now, if we look at the performance of this model, pretty much it's as I expected in here, that the model is not great with knowledge sorts of things.
[08:00] You can see here I asked it for a 5000 word essay. It's giving me a reasonably short amount of thinking on it, but the essay is nowhere near 5000 words, not even close in here. If we go in have a look at things like the logic puzzles, we'll see that this will fly along and it gets to the right answer here quite quickly. So, the logic stuff is actually going quite well. The same here for when we look at something like the hard math stuff, it's doing well. And that kind of reinforces what this model's actually
[08:31] good at, right? But if we come over and we look at something like the SVG Pelican test, this thing just doesn't do well at this kind of thing. You can see that we're starting to get really high token speeds on this, but when we actually look at the outputs, maybe they actually are sort of better as abstract art for certain things, but that's okay because that's not what the model's for.
[08:53] Exactly the same kind of thing when we look at the generating HTML page, right?
[08:58] If we look at the code that comes out, it's not that it's necessarily anything hugely wrong. It's just that it's not nowhere near as good as any of the other models that we would use for coding these days. That's not what this model is actually about. Where this model shines is going to be not at the coding outputs, but it's going to be at the tool use. So, you can see now I've moved over to the GGUF version. It's flying at well over 400 tokens per second, no problems here. But let's come over and
[09:29] look at how this actually does on the Agentech tests. Remember, these are a a of tests that test for function calling to see does it provide the right arguments to functions, is it able to use tool use well and stuff like that.
[09:45] And this is sort of like the basic set of tests here. And not only is it flying speed-wise here, it's getting all of them right. So we're getting some really nice results out of them. So then we come over to the more advanced one. So here the goal is to basically test things that often go wrong when you're running these kinds of things. And so again, I'm using the GGUF version. I'll just say going through this that the current setup with the SG Lang is actually seems to be the tool use seems to be returning the wrong format. So
[10:18] when I test out the Bfloat16 and the D Spark versions, I'm getting some issues.
[10:24] But it's pretty clear that they're not because of the model, just seems to be the configuration on SG Lang. When I try it on llama.cpp with the GGUF version, so this is a four-bit version, and of course with this you would expect that's actually going to be worse, it works fine. And you can see here that it's gone through and passed eight out of eight for all of these tests. Now some of the tests in here that I think are interesting are the sort of planning tests where it's using multiple different tools as it goes through. It
[10:53] has to deal with errors often before it gets to a correct output. With something like the prompt injection resistance, it has to work out how to basically extract out the right kind of thing going in here. And I'll point out while I'm showing you one run of these tests here, I run them at least 10 times going through. Now this one did fail a couple of the times, but it's been passing around about 80% of that. The other one too, which is one that a lot of models in the past used to have lots of problems with, were things like failure
[11:24] retry tests. So what's going on here is this is deliberately pretending to fail for two of the times, and to see does the model keep going at trying to get the result out. So, you can see that these failures were actually created deliberately. The model then keeps going. It's able to basically get the result out we want. The distractor tool bait also very impressive in that it's able to basically find the right information in there. If I come and test things out in the tool playground, here I think it's really interesting to see
[11:58] that this model has no problem both dealing with multiple tool calls here.
[12:03] But, you can see that this one it's having to use DuckDuckGo search and a fetch URL here. And it's lightning fast going through this. I'm getting sort of end-to-end over 500 tokens per second.
[12:15] And I guess partly because it's so small, it has no problems with large page pre-fills, being able to extract out what's the right information in there to pay attention to. Again, on multi-tool stuff, again we've got very fast result. We've got accurate use of the tools that are actually in there.
[12:32] And one of the things that I really like in here is that it's not trying to use its knowledge, right? The fact that it doesn't have the knowledge is fine as long as it doesn't pretend to have the knowledge. And we want it to actually defer to its tools rather than try to basically make everything up itself. So, for me this is the biggest issue with a model like this is that if we do actually give it a lot of tools and we ask it to defer to the tools rather than its own knowledge, does it have a problem with that or not? And it's great
[13:02] to see that if you give it the tools, it seems to know not to use its own knowledge. And that's exactly the kind of model that we want for sub agents, right? For anything that you're doing agentic where you've got sub agents with lots of tool calls, you don't want them trying to use their knowledge too much in there. You want them just getting the right stuff out for the right inputs for the different tools, etc. And that's exactly what's going on here with this model. Okay, so to finish up, there are
[13:31] a number of different versions that they've got of this up there. The ones that I showed in the video were the standard one, the GG UF, and the G Spark one. Definitely, this is worth checking out if you're looking for a very, very quick model that needs to deal with perhaps a little bit more complicated function calling and tool use than the previous 1B model. You can see in here that its strength has really been trained and pushed like crazy for this tool use. And while it also did well in the math reasoning and some of the other
[14:03] instruction following stuff that I gave it, the way that I would suggest that you use the model is for sub agents and for where you need a very quick but reasonably simple kind of model that can handle a lot of different tools and can deliver good arguments or the appropriate information to pass into those tools. So anyway, let me know in the comments what you think of it. As always, if you found the video useful, please click like and subscribe, and I will talk to you in the next video. Bye for now.
