---
title: "Claude Code 必學設定 Hooks，完整教學一次搞懂"
type: raw-transcript
created: 2026-08-18
video_id: rLNGSDYkK-w
url: https://www.youtube.com/watch?v=rLNGSDYkK-w
duration: 00:19:56
language: zh-TW
auto_generated: false
source_api: youtube_transcript_api
segments: 26
---

# Claude Code 必學設定 Hooks，完整教學一次搞懂

> Duration: 00:19:56 | Segments: 26 | Language: zh-TW | Source: API

## Transcript

[00:00:00] 上一支介紹 CLAUDE.md 的影片裡，我們學到 Claude Code 每次開始工作時都會讀取 CLAUDE.md 所以這份檔案很適合拿來放專案資訊 或是你的個人偏好 但有些狀況是 明明 CLAUDE.md 裡面寫了改完程式一定要跑測試 或是準備執行危險的 Git 指令時一定要先停下來 Claude 卻還是常常忘記 如果你感到心有戚戚焉 那是因為 CLAUDE.md 就像是給 AI 的一個提醒紙條而已 它提供了指示，Claude 會盡力遵守 卻不保證每一次都會乖乖照做 如果你希望有一套更強制，更穩定的機制的話 這就是 Hooks 可以幫你的地方 這支影片我會帶你完整搞懂 Hook 到底是什麼 它和 Claude Code 裡面其他功能有什麼差別 你會知道 Hooks 有哪幾個種類 而這幾個種類的 hooks 適合做什麼樣類型的工作？
[00:00:45] 如果你好奇別的大神都是怎麼樣使用 Hooks 的話 我也會拆解熱門的 GitHub repo 分析給你聽他們都是怎麼樣去使用 Hooks 的 最後，我會用實際案例帶你一步步用 Claude Code 建立屬於你的第一個 Hook 不論你是 Claude Code 還是 Codex 用戶 Hook 的原理都是通用的 因此看這支影片之後 你要在哪個 agent 裡面建立都可以 那我們就開始今天的影片 首先，我們來聊聊，到底什麼是 Hook？
[00:01:08] 你可以把它想像成便利商店的自動門 它背後有一套非常死板，絕對會執行的規則 只要有人走到感應區，門就立刻打開 完全沒有商量的空間 Hooks 在 Claude Code 裡的角色也是一模一樣的 你可以設定只要 Claude 一改完程式碼 測試就自動跑起來 你會發現 這句話的前半段是在定義觸發的時機與情境 而後半段是觸發後要做什麼 所以說穿了 Hook 就是把這兩件事情緊緊綁在一起的自動化設定 你定義好一個特定的情況 以及這個情況發生時你的 agent 要去做什麼事情 Hook 跟上一支提到的 Claude Markdown 最關鍵的差別在於是誰來負責啟動 CLAUDE.md 是讓模型自己去看 這代表它必須依賴 AI 自己去判斷什麼時候該遵守 但 Hook 完全不一樣 Hook 是 deterministic 的 它是由 Claude Code 這套軟體在背後掌控的強制機制 只要設定的時間點一到 軟體就會直接介入，強制執行任務 而不是靠模型自己去判斷要不要做 所以幫大家簡單歸納一下 如果只是一次性的任務 你在對話的時候直接講就可以了 如果是專案通用的規則跟大方向 很適合寫在 CLAUDE.md 裡面
[00:02:12] 讓模型當作參考 但如果這個動作是只要特定時機一到 就絕對必須嚴格執行 而且你一點都不想承擔被 AI 忘記的風險 那這時候，就非常適合把它做成 Hook 了 好，那一份真正的 Hook 設定檔到底長什麼樣子呢？
[00:02:28] 它其實只是用 JSON 格式寫的指令而已 通常會放在你專案資料夾裡面的 .claude/settings.json 檔案中 雖然看起來很像一堆密密麻麻的程式碼 但你完全不需要死背，也不用自己一行一行手打 只要搞懂它裡面的三層架構就可以了 我們直接舉個例子 假設有一個 Hook 的任務是檢查程式碼有沒有語法錯誤 如果你把這個設定檔打開 你會看到它裡面的分工非常明確，總共分成三層 第一層叫做 Event 它負責決定這個 Hook 要在什麼時候啟動 在這個語法檢查的例子裡面 我們的 Event 會設定成 PostToolUse 意思就是在 Claude 剛調用完工具的這個瞬間啟動 第二層叫做 Matcher 它是用來設定篩選條件的 Claude 在工作的時候會調用非常多種工具 但我們不可能每一次它調用工具 都把這個 Hook 叫出來執行 所以 Matcher 的功用 就是在告訴系統在這麼多的工作裡面 有哪些具體的操作，是真正要去篩選，去攔截的？
[00:03:24] 以這個語法檢查為例 我們就會把 Matcher 設定成只鎖定修改程式碼這個動作 最後第三層叫做 Handler 也就是真正採取行動的地方 當前面的條件都符合了 就由它來決定接下來要叫誰出來做事 以我們這個例子來說 Handler 就是去把你電腦裡面的語法檢查腳本叫出來 讓它自動幫你把錯誤抓出來 所以，如果要簡單總結這三層架構 你可以把它想成三個問題 Event 決定了什麼時候發生？
[00:03:52] Matcher 決定了具體要攔截哪個操作？
[00:03:55] Handler 決定了最後要做什麼動作？
[00:03:57] 這就是 Hook 最核心的運作邏輯 好，理解了 Hook 的基本架構跟極限之後 接下來，我們就來好好認識一下剛才提到的 Event 也就是啟動的時間點 老實說 Claude Code 裡面目前有多達三十一種 Event 聽起來很嚇人 但我們只要按照系統的工作階段來分類 抓出十個最核心的 Event 就夠了 我們先來看第一階段 也就是系統剛啟動 以及剛接收到你指令的時候 這個階段有兩個很重要的 Event 首先是 Session Start 顧名思義 它會在你開啟對話的那一瞬間觸發 不管你是開新對話，接續之前的紀錄 或是輸入 clear 指令清空畫面 只要對話環境一建立，它就會立刻啟動 這可以拿來做什麼呢？
[00:04:39] GitHub 上有個很熱門的專案叫做 Superpowers 作者就利用了 Session Start Hook 設定只要用戶一開啟對話 就強制 AI 載入 Superpowers 這項 Skill 他這麼做 正是為了對付 AI 載入 Skill 的隨機性 透過 Session Start Hook 就能確保用戶的每一個對話 都有確實載入這項技能 第二個 event 是 User Prompt Submit 它會在你按下 Enter 送出 prompt 的那一刻被觸發 這可以拿來做什麼？
[00:05:02] 我們以 Claude-Mem 為例 這是一個幫 Claude Code 建立長期記憶的開源專案 用來解決 AI 跨對話會失憶的問題 它就是利用 User Prompt Submit 這個 Hook 去攔截你送出的提問 在背後呼叫一支叫做 worker-service 的程式 讓這支程式先去背景資料庫 撈出跟這次對話最相關的記憶 然後再塞給 Claude Code 透過這個做法 就能強制確保 AI 在每一次對話前 都已經帶上你的相關記憶 接著進入第二階段 當 Claude 準備去修改檔案或下達指令的時候 它其實是透過呼叫背後的各種 Tools 來完成的 而這個階段的 hooks 就是發生在 Claude 已經決定好要用哪個工具 但還沒真的執行的空檔 所以非常適合用來做安全防呆 這裡最關鍵的 Event 就是 Pre Tool Use 這種 hook 會在工具正準備被調用前觸發 還記得我們之前介紹的 Matt Pocock Skills 嗎？
[00:05:48] 他在裡面就有一個 hook 來幫你擋掉危險的 Git 指令 它會用這個 hooks 去檢查 claude tool call 指令的內容 只要發現 Claude 試圖執行像 git reset --hard 這種會把程式碼洗掉的指令 或是危險的 git push 這個 hook 就會立刻攔截 當場中斷 Claude 的動作 並告訴它你沒有權限執行這個操作 接著我們進入第三階段 也就是 Claude 順利執行完 Tool call 之後的瞬間 這階段最常用的 Event 叫做 PostToolUse 非常適合用來做快速的驗收 我自己很喜歡一款叫做 Impeccable 的前端設計 Skill 它就是在這裡把關的 只要 Claude 一改完 UI 檔案 它就會立刻啟動幫你揪錯 它是怎麼檢查的呢？
[00:06:27] 舉例來說 如果 Claude 寫了圖片標籤 但裡面的圖片連結卻是空的 這在網頁上會變成一塊無法顯示的錯誤圖示 Impeccable 的這個 hook 就會透過掃描程式碼 立刻把這個問題抓出來 又或者是字體顏色太淡看不清楚 它會直接去計算文字跟背景顏色的數學對比值 一旦發現不符合標準 它就會自動跟 Claude 說 Claude 就會自動幫你修好 最後我們來到第四階段 也就是當 Claude 執行完所有任務 準備停下來等你 或是遇到一些特殊狀況的時候 這個階段有一個很關鍵的 Event 叫做 Stop 它會在你這一回合的對話完全結束時觸發 我們再拿剛才的 Impeccable 來說 如果你去看它的原始碼，會發現它把 排版節奏，配色和諧度 這種比較深度的美感檢查 刻意留到 Stop 階段才做 為什麼呢？
[00:07:11] 因為它會把你這整個工作階段裡 所有改過的檔案全部統整起來做一次總體檢 如果每改一行程式碼就跑這種深度檢查 開發速度會被嚴重拖慢 所以把它放到最後 Stop 的時候再一次做完 是最聰明的做法 我之前有出一部 cross model review 的影片 其實就是利用這個 stop event 讓 Claude 在寫完 plan 觸發 stop event 的時候 去呼叫 codex 進行 peer review 除了 Stop 之外 這階段還有幾種處理特殊狀況的 Event 第一個是很多人都會用的 Notification 也就是通知提醒 你可以把它設定成桌面通知或提示音 這樣 Claude 在背景工作時 你不用一直守在電腦前 可以先去泡杯咖啡，或處理別的事 等它需要你確認權限，回答問題時 電腦就會發出提示，把你叫回來 第二個是 Subagent 系列的 Event 主要有 SubagentStart 和 SubagentStop 當主 Claude 叫一個 subagent 開始工作時 就會觸發 SubagentStart 當這個 subagent 做完，準備交回結果時 就會觸發 SubagentStop
[00:08:09] 這兩個時機非常適合拿來控管 subagent 的工作品質 你可以在 subagent 開始前 先補上這次任務的規則和品質要求 等它做完後 再用 Hook 檢查產出有沒有達標 最後一個是 PreCompact 當對話太長時 Claude 會自動把前面的內容濃縮 騰出空間繼續工作 但如果你發現 每次濃縮之後 它總是會漏掉你最在意的資訊 例如關鍵決策，目前進度 或是不能更動的規則 你就可以使用 PreCompact Hook 在濃縮開始前 先把這些重要資訊整理並保存下來 這樣濃縮完成後 Claude 還是能接著前面的共識繼續工作 如果你是第一次接觸 先記住四個最常見的 Event 就好 SessionStart 會在對話開始或恢復時出現 PreToolUse 會在 Claude 準備使用工具 但還沒真的執行前出現 PostToolUse 會在工具成功執行之後出現 Stop 則是在 Claude 做完這一輪工作 準備停下來時出現 理解 Event 之後，接著來看第二個元件 Matcher 它的工作很簡單 就是從所有可能出現的動作裡 挑出這個 Hook 真正需要處理的目標 像這個 Hook 的 Matcher 顯示的是 Edit 和 Write
[00:09:12] 代表它只關心 Claude 修改檔案的動作 讀取檔案，搜尋資料，或執行其他工具時 它都不會繼續往下跑 但如果還想分得更細 下面還可以再加一個 if 條件 例如在這個案例中 它只會檢查副檔名是 .ts 的程式碼 當以上條件都符合之後 才會交給下一個 Handler 處理 Matcher 把目標挑出來之後 接著就輪到第三個元件，Handler 它會決定條件符合之後 這個 Hook 實際要做什麼 目前 Handler 總共有五種類型 分別是 command，http，mcp_tool，prompt 和 agent 接下來 我們一個一個來看它們適合處理哪些事情 第一個，也是最常用的 Handler，叫做 command 它的作用 就是讓 Hook 直接執行電腦上的指令或腳本 比如 Claude 改完程式碼之後 自動執行 lint 檢查 或是用 Prettier 把格式整理好 也可以在 Claude 準備執行指令之前 先跑一段檢查程式，把危險操作擋下來 接下來是 http 和 mcp_tool http 可以把 Hook 收到的資料傳到外部服務 比如工具執行失敗時 自動把錯誤訊息送到 Slack 讓團隊立刻知道
[00:10:18] mcp_tool 則可以直接使用已經連線的 MCP 工具 比如每次開始工作時 自動從 Jira 抓回今天的任務 交給 Claude 當作這次工作的背景資訊 最後兩種 Handler 是 prompt 和 agent 它們都會呼叫另一個 AI 來檢查條件有沒有符合 差別在於它們完成判斷的過程 先看 prompt 當 Hook 被觸發時 Claude Code 會把這次事件的資料 連同你事先寫好的檢查條件 一起交給另一個 AI 模型 這個 AI 只會根據收到的資料回答 不會自己打開檔案或搜尋其他資訊 例如 Claude 準備提交程式碼時 你可以請 prompt 檢查這次的 Commit 訊息 有沒有符合團隊格式 如果符合，就讓這次提交繼續 如果不符合，就把原因交回給 Claude 請它重新修改 agent 的運作方式更完整 當 Hook 被觸發時 它會叫起一個 subagent 並把這次的事件資料和驗收條件交給它 這個 subagent 可以先讀取檔案，搜尋程式碼 或是執行測試 查清楚實際狀況之後，再回傳驗收結果 例如 Claude 說功能已經完成 準備結束工作時 你可以請 agent 檢查修改過的檔案
[00:11:22] 對照原本的需求，再實際執行測試 如果全部符合，就讓 Claude 結束工作 如果發現缺少功能或測試失敗 就把原因交回給 Claude，讓它繼續修改 簡單來說 prompt 是拿著現有資料直接回答 agent 則可以先讀檔案，搜尋程式碼或執行測試 查清楚之後再回答 這裡要注意 每個 Event 支援的 Handler 類型都不太一樣 比如有些 Event 可以使用 prompt 和 agent 有些只能使用 command，http 或 mcp_tool 所以實際設定之前 記得先請 AI 查一下官方文件 確認你選的 Event 能不能搭配這個 Handler 到這裡 一個 Hook 的架構你應該已經清楚了 接下來 我會教你怎麼把需求講給 AI 聽 讓它幫你建立第一個 Hooks 要請 AI 幫你建立 Hook 其實先講清楚兩件事就夠了 它要在什麼時候啟動 以及啟動之後要做什麼 比如 當 Claude 準備提交程式碼時 先檢查這次的修改裡有沒有 E N V 檔案或 API 金鑰 如果有，就把這次提交擋下來 所以你表達的重點 前半段是啟動時機，後半段是要做的事 有了這兩個資訊 AI 就能先幫你建立第一版 Hook
[00:12:27] 接下來 我們就把剛才的需求直接講給 Claude Code 聽 你可以說請幫我在我的全域設定裡建立一個 Hook 每當你準備執行 Git Commit 時 先檢查這次要提交的內容 如果裡面包含 E N V 檔案 或是疑似 API 金鑰 就先把提交擋下來 並告訴我是哪一個檔案有問題 如果沒有發現，就讓提交正常繼續 完成之後 請測試有敏感資料會擋下 沒有敏感資料會放行這兩種情況 Claude 建立完成之後 會把這個 Hook 加進我的全域設定 它使用的 Event 是 PreToolUse 代表 Claude 每次準備執行工具之前 Hook 會先介入 它的 Matcher 是 Bash 所以只有 Claude 準備執行終端機指令時 才會叫起這支檢查程式 因為我把它放在根目錄之下的點 claude 資料夾 settings json 裡 所以不管我正在使用哪一個專案 只要 Claude 準備執行 Git Commit 都會經過這道檢查 接著看 Handler 它會叫起一支名為 Git Commit Secret Guard 的檢查程式 雖然 Matcher 會在每一個 Bash 指令執行前叫起它
[00:13:25] 但程式會先判斷這次有沒有要執行 Git Commit 如果和提交無關，它就安靜結束 只有真的要提交時 才會檢查這次要進入 Git 的內容 只要發現 E N V 檔案，私鑰，憑證 或是疑似 API 金鑰 就會擋下這次提交 並告訴我問題出在哪一個檔案 如果沒有發現問題，就讓提交正常繼續 接著我們直接試一次 我先請 Claude 提交一個包含 E N V 檔案的修改 你可以看到 Git Commit 還沒有真的執行 Hook 就先把它攔了下來 並指出是哪一個檔案有問題 把敏感資料移除之後再試一次 這次檢查通過，提交就可以正常繼續 剛才第一個例子 我們是用 Command Handler 執行一支檢查程式 因為 E N V 檔案和 API 金鑰都有明確的判斷規則 用程式比對就夠了 接下來 我想示範一種需要質化判斷的 Hook 這個例子來自我平常做內容時很常遇到的問題 Claude 寫出來的文章 資訊可能都對 讀起來卻有一股很重的 AI 腔 所以我希望 每次 Claude 寫完一篇文章 Hook 都能再幫我檢查一次 看看裡面還有沒有 AI 味 為了建立這個 Hook 我會直接對 Claude 說請幫我建立一個 Hook
[00:14:31] 每當你寫完一篇 Blog 文章 準備結束工作時，啟動一個 Agent 這個 Agent 要讀取你剛產出的文章 呼叫用來去除 AI 寫作痕跡的 Humanizer Skill 再檢查文章裡有沒有 AI 味 如果發現問題 就把有問題的段落和原因交回給你 請你繼續修改 檢查通過之後，才能結束工作 Claude 建立完成之後 會把這個 Hook 放進目前專案的 settings json 它使用的 Event 是 Stop 所以每當 Claude 準備結束這次工作時 Hook 就會先檢查這個過程中有沒有修改 Blog 文章 這次使用的仍然是 Command Handler 它會執行一支叫做 Humanizer Gate 的檢查程式 這支程式不會自己判斷文章有沒有 AI 味 它負責找出這次修改過 而且還沒通過檢查的文章 再要求 Claude 開一個 Agent 來審查 接著我們直接試一次 當 Claude 寫完文章，準備結束工作時 你可以看到 Hook 先把它攔下來 接著 Claude 會開一個 Agent 呼叫 Humanizer 中文版 Skill 讀完文章 再把有 AI 寫作痕跡的段落和原因交回來
[00:15:28] Claude 修改完成之後 Hook 會再檢查一次 確認通過，這次工作才會結束 如果文章後來又被修改 原本的通過紀錄也會失效 下次結束前還是要重新檢查 做完這兩個例子之後，你會發現 請 Claude 建立第一版 Hook 時 只要先說清楚什麼時候啟動和啟動後做什麼就夠了 但要讓這個 Hook 長期穩定使用 建立完成後還要再檢查兩件事 第一，觸發範圍夠不夠精確 Hook 的範圍如果設得太大 就會在很多無關的操作中被叫起 浪費時間，也會一直打斷工作 所以 Matcher 要先縮小範圍 Handler 裡面再檢查更細的條件 所以，我們剛才設定的 Git Hook 才會先用 Matcher 鎖定 Bash 再由檢查程式判斷這次執行的是不是 Git Commit 其他終端機指令雖然也會叫起檢查程式 但確認與提交無關之後 就會立刻安靜結束 第二，Stop Hook 有沒有設定結束條件 Stop Hook 每次阻止 Claude 結束 都會要求它繼續工作 等 Claude 修改完成，再次準備結束時 同一支 Hook 又會啟動 如果沒有明確的通過條件 它就可能一直退回，修改，再重新檢查 所以剛才的 Humanizer Gate 會記錄
[00:16:34] 目前這個版本的文章是否已經通過 通過之後 只要文章沒有再被修改，下一次就會直接放行 如果連續檢查三輪還是沒有通過 就停止退回，交給我人工確認 這樣才能避免整個工作卡在重複檢查裡 簡單來說，建立 Hook 時 先講清楚什麼時候啟動和啟動後做什麼 建立完成後 再確認它只會在需要的時候啟動 而且知道什麼時候該停止 這樣才適合長期放在你的工作流程裡 如果你看到這裡 知道 Hooks 可以解決什麼問題 但還不知道自己應該先裝哪一些 我把最常用的十五個情境整理成了一份 Hooks 實戰大全 裡面包含程式開發 內容創作和日常工作會用到的 Hook 每一個案例都有可以直接交給 AI 的建立 Prompt 你只要複製貼上 就能讓 AI 幫你完成設定 每個人的工作方式都不一樣 只看一份清單 你可能還是不知道哪些 Hook 最適合自己 所以我另外準備了一個 Hook 推薦 Prompt 它會分析你電腦裡過去所有的 Session 找出你經常重複提醒 AI 的事情 以及總是在固定時機執行的步驟 再根據你真實的使用習慣 推薦適合你的 Hooks 完整內容我放在 Patreon 需要的朋友可以在影片下方資訊欄找到連結
[00:17:44] 到這裡 我們已經用 Claude Code 完整做了兩個 Hook 如果你平常主要使用 Codex 前面學到的判斷方式一樣可以沿用 先說清楚什麼時候啟動 以及啟動後要做什麼 不過兩邊目前支援的 Event 和 Handler 不完全相同 所以 Claude Code 的設定不能直接複製到 Codex 用 Codex 的朋友 你只要記住兩個差異 第一個差異，是 Event 的數量 Claude Code 目前有三十一種 Codex 則有十一種 不過像剛才用到的 PreToolUse 和 Stop 兩邊都有支援 所以 E N V 檢查和 Humanizer Gate 的基本做法 都能搬到 Codex 實際建立時 你不用自己背這些 Event 的名稱 只要講清楚希望它在什麼時候啟動 Codex 就會根據目前支援的 Event 判斷這個需求能不能做到 第二個差異，是 Handler Claude Code 支援 command，http，mcp tool prompt 和 agent Codex 目前真正會執行的只有 command 不過 這不代表 Codex 不能完成需要 AI 判斷的流程 像剛才的 Humanizer Gate
[00:18:38] 本身就是 Command Handler 先執行檢查程式 再要求主要的 Agent 呼叫 Skill 完成審查 所以同一個需求通常還是做得到 只是背後的串接方式會不同 最簡單的做法 就是把你想解決的問題直接告訴 Codex 請它按照目前支援的格式重新建立 不要直接複製 Claude Code 的設定 講到這裡 相信你已經對 Hooks 有一個完整的理解 它可以在固定的時機 自動替你執行檢查，通知或防呆 Event 決定什麼時候啟動 Matcher 決定哪些情況需要處理 Handler 則負責啟動之後要做什麼 實際建立時 你也不需要自己背設定或手寫程式 先找出一件你經常提醒 AI 而且每次都發生在固定時機的事情 接著把什麼時候啟動和啟動後做什麼講清楚 讓 AI 幫你建立第一版 再確認它的觸發範圍和結束條件 你可以從一個很小的問題開始 像是提交程式碼前檢查金鑰 只要這件事會重複發生 而且適合在固定時機自動處理 它就值得做成 Hook 你一個一個把這些重複提醒交給 Hook 會發現很多原本需要自己記得 反覆確認的事情 慢慢變成工作流程裡會自動發生的一部分 而你也可以把注意力留給真正需要自己判斷的事情
[00:19:44] 今天這支影片我們團隊光是研究還有撰寫腳本 就花了快要一週的時間 如果對你有所幫助 希望可以幫我留言，按讚，熱推 以及分享給你的朋友 這些就是對我最大的支持，那我們下次見